only_rules:
  - colon
  # - indentation_width
  - custom_rules

# TODO: Zach - apply format then enable this rule
# indentation_width:
#   indentation_width: 2

custom_rules:
  no_init_shorthand:
    name: "No .init shorthand"
    regex: "(?<!super)(?<!self)(?<!Self)\\.init\\("
    message: "Avoid using .init shorthand. Use the full type name instead."
    severity: error

  # sf_safe_symbol:
  #   name: "Safe SFSymbol"
  #   message: "Use `SFSafeSymbols` via `systemSymbol` parameters for type safety."
  #   regex: "(?<!\\w)(Image\\(systemName:|NSImage\\(symbolName:|Label\\([^,]+?,\\s*systemImage:|UIApplicationShortcutIcon\\(systemImageName:)"
  #   severity: error